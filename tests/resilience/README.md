# Resilience checks

Database backup/restore and worker retry/lease checks remain with their owning
platform components under `platform/database/integration/` and
`platform/worker/integration/`. This index records the intended cross-boundary
test home without duplicating those fixtures.
