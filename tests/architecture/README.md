# Architecture checks

The executable boundary check is currently
[`../../scripts/verification/check-architecture-boundaries.sh`](../../scripts/verification/check-architecture-boundaries.sh)
with its Node regression tests beside the script. This directory is the public
index for architecture verification; it does not duplicate the source test
harness.
