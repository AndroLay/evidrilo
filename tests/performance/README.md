# Performance checks

No production-scale performance claim is made yet. Future benchmarks must
define dataset size, device or server environment, warm-up, threshold, and
repeatable output before they are accepted as evidence.
