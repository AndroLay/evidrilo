# RevenueCat, Compose, and Kotlin publish the consumer rules needed by the
# shared application. Keep this file for app-specific rules without applying
# a broad keep-all policy that would defeat release shrinking.
