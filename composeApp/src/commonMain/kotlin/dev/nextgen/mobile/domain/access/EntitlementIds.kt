package dev.nextgen.mobile.domain.access

/**
 * Canonical product capability identifier understood by the application
 * boundary. The RevenueCat adapter may map provider state to this identifier,
 * but the domain must not depend on the provider SDK or billing package.
 */
const val EVIDRILO_PRO_ENTITLEMENT_ID = "evidrilo_pro"
