# Formatting tooling boundary

Repository formatting is currently governed by `.editorconfig` and the native
toolchain defaults. Add a formatter only with a pinned version and a CI check
that does not rewrite unrelated files.
