# Architecture-rule tooling

The current executable rule is
[`../../scripts/verification/check-architecture-boundaries.sh`](../../scripts/verification/check-architecture-boundaries.sh).
This directory documents the ownership of future rule implementations; the
existing script remains the compatibility entry point.
