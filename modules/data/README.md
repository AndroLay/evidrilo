# Data module boundary

Reserved for persistence, documents, network, AI, sync, and billing adapters.
These boundaries are platform-sensitive and remain in the current shared app
until each extraction has an explicit interface and regression coverage.
