# Examples

This directory is reserved for small, synthetic examples that demonstrate a
public contract without requiring credentials, provider access, private content,
or a running managed service. No example fixture is published yet.
