# Event contract boundary

No public event envelope is defined yet. RevenueCat webhook input and internal
projection messages remain behind their server-side boundaries; they must not be
published here until redaction, authentication, ordering, replay, and version
rules are explicitly approved.
